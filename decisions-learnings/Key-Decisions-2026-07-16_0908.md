# Key Decisions & Learnings — 2026-07-16 09:08 UTC (Root Level)

**Session:** One-time system setup + app integration directive
**Model:** claude-fable-5
**Scope:** AI Memory System

---

## Decisions

### Decision 1: One-time setup executed with known information; interactive items deferred
- **Context:** Owner requested "do a one-time system set up". The setup protocol is designed as an interactive walkthrough (owner identity, API keys, enterprise OS, model preferences, standards customization), but this session runs autonomously.
- **Options Considered:** (a) Refuse until an interactive session is possible; (b) execute with available facts and explicitly defer credential/preference items.
- **Decision:** Option (b).
- **Rationale:** Owner identity (email), fork URL, and model-in-use are already known facts; deferring only the genuinely unknowable items (API keys, enterprise OS, standards customization) completes the setup without inventing data.
- **Impact:** Master-AI-Context.md, README.md, agents/open.md, agents/closure.md configured. NextSteps.md carries the deferred items.

### Decision 2: No keys file created
- **Context:** Setup Step 2.6 generates a `{Name}Keys.txt` from collected credentials.
- **Decision:** Skipped — no credentials were provided; documented the convention (`FataliKeys.txt`, git-ignored) in Master-AI-Context.md instead.
- **Rationale:** Secrets must never be committed; a local placeholder in an ephemeral cloud container would not persist anyway.
- **Impact:** Owner adds keys locally when ready.

### Decision 3: Standards adopted as-is (Option A)
- **Context:** Setup Step 4 offers use-as-is / customize / defer.
- **Decision:** Use as-is, customization available any time later.
- **Rationale:** Recommended default; no owner preferences supplied.
- **Impact:** CC/DE v2.0 and ACS v1.0 root copies unchanged.

### Decision 4: App integration directive recorded at project level
- **Context:** Owner said "let integrate with all kind of tools" — this answers the staging project's open integration question.
- **Decision:** Recorded as an owner-confirmed constraint in the staging Master-Context.md (integration-first, API-first architecture with connector modules); Document 2 moved to In Progress.
- **Rationale:** Owner directive; project-level decision logged in the project's own decisions-learnings.
- **Impact:** See `projects/staging/FatalibuildersConstructionApp/decisions-learnings/Key-Decisions-2026-07-16_0908.md`.

## Learnings

- **Learning 1:** The fresh-install guard in setup-AI-Memory.md returns FALSE once any session has run, even if the system was never configured. The guard's intent (don't re-configure a configured system) was honored by checking that owner/repo fields were still placeholders before proceeding.

---
*Finalized by closure protocol.*
