# Key Decisions & Learnings — 2026-08-01 00:14 UTC (Root Level)

**Session:** Intake of a new subject, "Deriv" — no prior reference in the memory system
**Model:** claude-opus-5
**Scope:** AI Memory System

---

## Decisions

- **Session classified as root-level.** "Deriv" maps to no existing project workspace, so the root protocol (`agents/open.md`) applies rather than any project's own open protocol.
- **Held intake open rather than inventing a vision.** `staging.md` Phase 1 (Steps 1.1–1.4) requires the project name, category, vision, and owner to come from the user. Proceeding on a guess would put fabricated content at the root of Document 1, which every downstream document derives from.

## Learnings

- **The memory system has no "Deriv" entry.** A full-repository search returns only the ordinary word *derive/derived* in unrelated documents (Investment agent valuation steps, the Project Magic white paper plan, the Fatalibuilders architecture note on derived outputs). Confirms this is a genuinely new subject, not a continuation of prior work.
- **Single-word task briefs are ambiguous by construction.** A bare project-name-shaped brief cannot distinguish "stage this as a new project" from "answer a question about it". The staging protocol's Phase 1 intake is the system's own designed remedy — run it rather than resolving the ambiguity unilaterally.

---
*Live file — updated incrementally. Finalized by closure protocol.*
