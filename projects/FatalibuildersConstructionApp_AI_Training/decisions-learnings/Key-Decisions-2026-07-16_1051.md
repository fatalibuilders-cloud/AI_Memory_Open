# Key Decisions & Learnings — 2026-07-16 10:51 UTC

**Session:** First build session — Release 1.0 Epic 1 (CORE-1.0 scaffold)
**Model:** claude-fable-5

---

## Decisions
*(appended in real time)*

### 1. Temporary in-repo scaffold location (CORE-1.0 deviation)
- **Decision:** The GitHub integration returned 403 on repository creation (cannot create new repos). The app is scaffolded at `app-src/fatalibuilders-app/` inside the AI_Memory_Open fork, clearly marked TEMPORARY, instead of its own repository.
- **Rationale:** The session container is ephemeral — unpushed work would be lost. Scaffolding in the memory repo preserves the code; migration is a single `git push` once the owner creates the empty repo.
- **Impact:** New `[Human]` checkpoint added to NextSteps: owner creates the empty GitHub repository `fatalibuilders-app` (plain-language steps provided). CORE-1.0 marked [IN-PROGRESS] until migration completes; all other acceptance criteria verified in place.

## Learnings
*(appended in real time)*

### 1. GitHub App permissions in this environment
- The `create_repository` MCP tool 403s — the integration is scoped to existing repositories. Future repo-creation stories must be planned as `[Human]` checkpoints (owner clicks "New repository" on GitHub) followed by `add_repo`.

---
*Live file — updated incrementally. Finalized by closure protocol.*
