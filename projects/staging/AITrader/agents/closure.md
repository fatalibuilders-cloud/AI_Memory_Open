# closure.md — Session Closure Protocol (AITrader — Staging)

**Purpose:** Execute this file at the end of any session working on AITrader while it is in the staging phase.

---

> **Prompt for AI:** "Execute the AITrader staging session closure protocol:
>
> 1. **Log Decisions:** For each decision made this session, create a `decisions-learnings/[YYYY-MM-DD]_[slug].md` file per `decisions-learnings/session-protocol.md`, and add a row to `Key-Decisions.md`.
> 2. **Update Master-Context.md:** Update the Staging Roadmap status checkboxes for Documents 1-3, and fold any new content into the relevant document section.
> 3. **Update NextSteps.md:** Mark completed items, add new ones surfaced this session.
> 4. **Update Sessions.md:** Add a row for this session with date and keywords.
> 5. **Push to git:** commit with a clear message and push.
> 6. **If all three documents are complete and approved:** prompt the user to run `PROJECT_MEMORY_INIT.md` to promote AITrader to a full project."
