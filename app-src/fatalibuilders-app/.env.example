# Copy to .env.local for local overrides. NEVER commit real values.

# Managed PostgreSQL connection string (set in production at CORE-1.2).
# When unset, the app uses an embedded local database (.pglite/) — fine for development.
DATABASE_URL=

# Public URL of the deployed app (used for redirects). Defaults to http://localhost:3000.
APP_URL=
